# Separation engine, CPU build.
#
# Installed when no NVIDIA GPU was detected. The CPU wheels are roughly a
# third the size of the CUDA ones and are what most people will get.
#
# Demucs is archived upstream (facebookresearch/demucs is read-only; the
# author's fork takes critical fixes only), so the upper bound on torch
# matters more than usual — a new major release is far more likely to
# break Demucs than to be supported by it.

--extra-index-url https://download.pytorch.org/whl/cpu

torch>=2.1,<3
torchaudio>=2.1,<3
demucs>=4.0.1,<5
